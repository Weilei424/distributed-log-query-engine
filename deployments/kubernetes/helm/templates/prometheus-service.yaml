{{- if .Values.prometheus.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: logengine
spec:
  selector:
    app: prometheus
  ports:
  - port: {{ .Values.prometheus.port }}
    name: http
{{- end }}
